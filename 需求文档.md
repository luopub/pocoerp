# PocoERP 需求文档（v1.2 定稿）

> 面向小规模跨境电商卖家、以产品管理为核心的轻量级 ERP 系统。
> 状态：**需求定稿，待开工确认** —— 用户明确确认后开始开发。

---

## 1. 项目背景与目标

小规模跨境电商卖家，核心诉求是管理**产品成品库存**，并覆盖两条供给链路：

1. **直接采购成品**：从供应商购买成品 → 入库。
2. **委外加工**：自购原材料 → 多道供应商工序加工 → 成品入库。

发货去向为电商平台或直发客户，第一期**手动出库**。

系统目标：清晰掌握"有多少货、货怎么来的、成本多少、发到哪去了"。

---

## 2. 已确认的关键决策

| 项目 | 决策 |
|------|------|
| 产品结构 | 单一原材料为主：一件成品主要由一种原材料加工，记录用量即可，不做复杂 BOM |
| 出库方式 | 第一期只做**手动出库**，预留平台对接扩展 |
| 出库渠道 | 出库时记录**去向渠道**（渠道可自定义维护），支持按渠道统计 |
| 成本核算 | 需要：记录采购价、加工费，按**移动加权平均**核算成品单位成本 |
| 工序流转 | 简单记录：加工单记录每道工序的供应商、开始时间、投入/产出数量；半成品不建档 |
| 仓库 | **单仓**，发货即出库，不跟踪在途 |
| 用户权限 | 登录体系 + **多角色权限** |
| 技术栈 | **Node.js**，云服务器部署，网页版 |
| 附加功能 | 导出 Excel、产品图片、库存预警（不需要多币种） |
| 业务规模 | 中等：SKU 100~500，每月单据几百张 |
| 数据库 | **MongoDB** |
| 角色方案 | 管理员 / 仓管 / 只读 三种 |

---

## 3. 角色与权限（建议方案，可调整）

| 角色 | 权限 |
|------|------|
| 管理员 | 全部功能 + 用户管理 + 基础数据维护（渠道、仓库参数等） |
| 仓管 | 产品/原材料/库存/采购/加工/出库的日常单据操作 |
| 只读 | 查看库存、单据、报表，不能修改 |

第一期账号由管理员在后台创建，不开放注册。

---

## 4. 功能模块详细需求

### 4.1 登录与用户管理
- 用户名 + 密码登录，会话保持（Token / Cookie）。
- 管理员可创建、停用、重置密码、分配角色。

### 4.2 产品（成品）管理
- 字段：SKU（唯一）、名称、规格、单位、图片、**安全库存阈值**、备注、状态（启用/停用）。
- 每个产品标记来源方式：直接采购 / 委外加工 / 两者皆可。
- 委外加工类产品需配置：**原材料 + 单位用量**（如 1 件成品 = 0.5 kg 布料）、**工序模板**（工序名称列表，如：裁剪 → 印花 → 缝制）。
- 列表支持搜索、筛选、导出 Excel；库存低于安全库存时醒目标记。

### 4.3 原材料管理
- 字段：编码（唯一）、名称、单位、默认供应商、安全库存阈值、备注。
- 原材料库存实时数量 + 移动加权平均成本。
- 库存预警同产品。

### 4.4 供应商管理
- 字段：名称（唯一）、类型（成品供应商 / 原材料供应商 / 加工商，可多选）、联系人、电话、备注。

### 4.5 采购管理

#### 4.5.1 成品采购单
- 单头：单号（自动生成）、供应商、采购日期、备注。
- 明细：产品、数量、单价。
- 状态流转：**待入库 → 已入库**（可作废）。
- 确认入库时：成品库存增加，按移动加权平均更新成本。

#### 4.5.2 原材料采购单
- 结构同上，入库时增加原材料库存并更新加权成本。

### 4.6 委外加工管理（加工单）
- 单头：单号、成品、计划数量、创建时间、状态。
- **发料**：按产品配置的用量自动计算应发原材料数量（可手工调整），确认后原材料库存扣减，记录发料时间。
- **工序记录**（按产品工序模板生成，顺序执行）：
  - 每道工序记录：加工供应商、**开始时间**、**投入数量**、完成时间、产出数量、加工费（单价 × 数量 或 总额）、备注。
  - 当前工序标记"完成"后解锁下一道工序。
- **完工入库**：最后一道工序完成后，按最终产出数量成品入库。
- **加工成本核算**：该单总成本 = 发料成本（原材料加权成本 × 发料量）+ 各工序加工费之和；单位成本 = 总成本 ÷ 入库数量，并入移动加权平均。
- 状态流转：**待发料 → 加工中（第 N 道工序）→ 已完成**（可作废）。
- 列表可查看所有在制工单及当前工序进度。

### 4.7 库存管理（核心）
- 成品、原材料实时库存（数量 + 加权平均成本 + 库存金额）。
- **库存流水**：每一次变动一条记录——时间、类型（采购入库/发料出库/完工入库/销售出库/其他）、关联单据、变动数量、变动后结存、操作人。
- 流水可按产品/类型/时间范围筛选，可导出 Excel。

### 4.8 出库管理（手动）
- 出库单：单号、产品、数量、**去向渠道**（下拉，渠道可在设置中维护）、出库日期、关联平台单号/备注（可选）、操作人。
- 确认后扣减库存（不允许扣成负数，给出提示）。
- 支持作废（回滚库存）。

### 4.9 报表 / 看板
- **首页看板**：库存预警列表（低于安全库存的产品/原材料）、在制工单数、本月入库/出库汇总。
- 库存总览表（数量、成本、金额）。
- 按**渠道**统计出库量（按时间段）。
- 成本查询：每个产品当前加权成本及近期入库成本记录。

### 4.10 基础设置
- 渠道维护（增删改，如 Amazon-US、TikTok、线下客户）。
- 用户管理（管理员）。
- 数据导出（Excel）。

### 4.11 编号规则（统一）

格式：**3 位字母前缀 + 6 位顺序号**，子号码在主号码后加 `-<3位数字>`。

| 对象 | 前缀 | 示例 |
|------|------|------|
| 产品 | PRD | PRD000001 |
| 原材料 | MAT | MAT000001 |
| 供应商 | SUP | SUP000001 |
| 成品采购单 | POP | POP000001 |
| 原材料采购单 | POM | POM000001 |
| 加工单 | WKO | WKO000001 |
| 出库单 | OUT | OUT000001 |
| 采购单明细（子号） | 主号-序号 | POP000001-001 |
| 加工单工序（子号） | 主号-序号 | WKO000001-002 |

- 序号全局连续、自动生成，不允许手工修改，作废单据不占新号也不回收号。
- 渠道、库存流水、用户不编号（流水按时间排序）。

---

## 5. 数据模型草案（MongoDB 集合）

```
users           用户（用户名、密码哈希、角色、状态）
products        产品（SKU、名称、规格、单位、图片、安全库存、来源方式、
                原材料ID、单位用量、工序模板数组、状态）
materials       原材料（编码、名称、单位、安全库存）
suppliers       供应商（名称、类型、联系方式）
channels        渠道（名称）
purchase_orders 采购单（单号、类型[成品/原材料]、供应商、日期、状态，
                明细内嵌数组：物品、数量、单价）
work_orders     加工单（单号、产品、计划数量、状态、发料数量、发料时间，
                工序记录内嵌数组：顺序、供应商、开始时间、投入数量、
                完成时间、产出数量、加工费）
outbound_orders 出库单（单号、产品、数量、渠道、日期、备注）
inventories     库存（物品类型、物品ID、数量、加权成本）
inventory_logs  库存流水（时间、类型、单据ID、变动数量、结存、操作人）
```

> 说明：采购明细、加工单工序记录采用 MongoDB 内嵌数组，随单据一次读写；
> 库存扣减 + 流水写入放在同一事务中，保证一致性。

---

## 6. 技术方案建议（可调整）

| 层 | 选型 | 理由 |
|----|------|------|
| 后端 | Node.js + Express + Mongoose | 轻量、开发快、生态成熟 |
| 前端 | Vue 3 + Element Plus | 中后台管理系统标配，表格/表单组件丰富 |
| 数据库 | MongoDB（**已有实例**，连接参数见部署环境变量） | 用户指定；需确认实例已开启副本集以支持多文档事务（保证库存扣减与流水写入的一致性），开发第一步验证 |
| 认证 | JWT + bcrypt 密码哈希 | 标准方案 |
| 图片 | 服务器本地存储（uploads 目录） | 规模小，无需对象存储 |
| Excel 导出 | exceljs | Node 生态成熟 |
| 部署 | 已有 Ubuntu 22.04 服务器；PM2 + Nginx（或 Docker） | 连接参数通过环境变量注入，不写入代码库 |

### 6.1 部署环境

- **服务器**：Ubuntu 22.04.5，SSH 连接参数在环境变量中：
  `MYDATASERVER_IP` / `MYDATASERVER_PORT` / `MYDATASERVER_SSH_USER` / `MYDATASERVER_PASSWORD`
- **MongoDB**：已有实例，参数在环境变量中：
  `MONGODB_HOST` / `MONGODB_PORT` / `MONGODB_DBNAME` / `MONGODB_USERNAME` / `MONGODB_PASSWORD`
- **安全要求**：以上敏感信息只通过环境变量 / 服务器端 `.env`（不提交代码库）注入；
  代码仓库中只保留 `.env.example` 模板。

---

## 7. 非功能需求

- 界面语言：中文。
- 响应式以桌面浏览器为主，兼顾平板；不做原生 App。
- 每日自动备份数据库到服务器本地目录（保留 7 天）。
- 关键单据操作有确认提示；作废保留痕迹，不做物理删除。

---

## 8. 第一期范围 vs 后续规划

**第一期（本次开发）**：第 3、4 章全部功能。

**后续预留**（不在本期，但数据模型不挡路）：
- 电商平台 API 对接自动出库
- 多仓库 / FBA 在途跟踪
- 销售收入与利润分析
- 扫码、多币种、批次管理

---

## 9. 遗留小问题

全部解决 ✅

1. ~~角色~~ ✅ 管理员 / 仓管 / 只读 三种
2. ~~数据库~~ ✅ MongoDB（已有实例）
3. ~~云服务器~~ ✅ 已有，Ubuntu 22.04.5，SSH 与 MongoDB 参数均在环境变量中
4. ~~编号规则~~ ✅ 3 位字母前缀 + 6 位序号，子号 `-<3位数字>`（详见 4.11）

---

## 10. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-08-13 | 根据初次描述创建的草稿 |
| v0.2 | 2026-08-13 | 确认：单一原材料、手动出库、成本核算、云服务器部署 |
| v0.3 | 2026-08-13 | 确认：工序简单记录、单仓、移动加权平均成本、多角色权限 |
| v1.0 | 2026-08-13 | 确认：渠道统计、Node.js、导出 Excel/图片/库存预警、中等规模；重写为完整确认稿 |
| v1.1 | 2026-08-13 | 确认：MongoDB、三种角色方案；数据模型调整为集合结构 |
| v1.2 | 2026-08-13 | 确认：统一编号规则（4.11）、部署环境（已有 Ubuntu 22.04 服务器与 MongoDB 实例，参数走环境变量）。**需求定稿，待开工确认** |
