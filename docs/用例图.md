# PocoERP 用例图（基于需求文档 v1.14）

> 按功能优先级分 3 组：P1 核心业务闭环 → P2 日常运营增强 → P3 管理与分析。
> Mermaid 无原生 UML 用例图，使用 flowchart 模拟：方框 = 角色（Actor），圆角框 = 用例（Use Case）。

## P1 核心业务闭环（系统跑起来的最小集合）

```mermaid
flowchart LR
    admin[👤 管理员]
    keeper[👤 仓管]
    viewer[👤 只读用户]

    subgraph P1[P1 核心业务闭环]
        uc_login([登录系统])
        uc_user([用户与角色管理])
        uc_channel([渠道维护])
        uc_supplier([供应商建档])
        uc_product([产品/SKU 建档<br/>BOM·工序模板·图片·耗材成本])
        uc_material([原材料/SKU 建档])
        uc_mapping([平台映射维护<br/>默认映射自动生成])
        uc_import([期初数据导入 Excel])
        uc_purchase([采购入库<br/>成品/原材料])
        uc_work([委外加工全流程<br/>按SKU计划·按工序发料<br/>工序记录·完工入库·成本核算])
        uc_inv([库存查询<br/>SKU级·虚拟组合派生])
        uc_logs([库存流水查询])
        uc_out([销售出库<br/>按映射·自动带图<br/>虚拟组合展开扣减])
    end

    admin --> uc_login
    keeper --> uc_login
    viewer --> uc_login
    admin --> uc_user
    admin --> uc_channel
    admin --> uc_import
    keeper --> uc_supplier
    keeper --> uc_product
    keeper --> uc_material
    keeper --> uc_mapping
    keeper --> uc_purchase
    keeper --> uc_work
    keeper --> uc_inv
    keeper --> uc_logs
    keeper --> uc_out
    viewer --> uc_inv
    viewer --> uc_logs
```

## P2 日常运营增强（业务跑顺后很快需要）

```mermaid
flowchart LR
    keeper[👤 仓管]
    admin[👤 管理员]
    sys[⚙️ 系统自动]

    subgraph P2[P2 日常运营增强]
        uc_partial([分次入库与差异结案])
        uc_stocktake([盘点<br/>分批·最后盘点时间])
        uc_return([退货入库<br/>良品回库·不良品转报废])
        uc_scrap([报废出库·损耗记录])
        uc_alert_static([安全库存预警])
        uc_alert_dynamic([动态可用天数预警<br/>三级参数覆盖])
        uc_replenish([补货建议<br/>一键生成采购/加工单草稿])
        uc_overdue([工序超期提醒])
        uc_settings([全局预警参数设置])
    end

    keeper --> uc_partial
    keeper --> uc_stocktake
    keeper --> uc_return
    keeper --> uc_scrap
    sys --> uc_alert_static
    sys --> uc_alert_dynamic
    sys --> uc_overdue
    keeper --> uc_replenish
    admin --> uc_settings
```

## P3 管理与分析（管理决策支撑）

```mermaid
flowchart LR
    admin[👤 管理员]
    keeper[👤 仓管]
    viewer[👤 只读用户]

    subgraph P3[P3 管理与分析]
        uc_payment([付款登记<br/>未付/部分/已清])
        uc_statement([供应商对账<br/>欠款天数·导出])
        uc_turnover([SKU 平均库存周期报表])
        uc_channel_stat([渠道出库统计])
        uc_cost([成本查询<br/>加权成本·入库历史])
        uc_export([Excel 导出])
    end

    keeper --> uc_payment
    admin --> uc_payment
    admin --> uc_statement
    keeper --> uc_statement
    viewer --> uc_turnover
    viewer --> uc_channel_stat
    viewer --> uc_cost
    admin --> uc_export
    keeper --> uc_export
```

## 分组依据

| 组 | 标准 | 对应需求章节 |
|----|------|--------------|
| P1 | 没有它业务闭环走不通（建档→入库→加工→库存→出库） | 4.1~4.6、4.7 主功能、4.8 销售出库、4.12、4.13 |
| P2 | 不影响开单，但日常运营效率和准确性依赖它 | 4.5 分次入库、4.7.1 盘点、4.14、4.8 报废、4.9 预警/补货/超期 |
| P3 | 周期性管理动作与决策分析 | 4.5/4.6 付款、4.9 对账/周转/渠道统计、4.10 导出 |

> 建议开发顺序也按 P1 → P2 → P3 推进，每期结束都可用第 9 章对应情景验收。
